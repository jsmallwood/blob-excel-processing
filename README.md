# blob-excel-processing
Azure Data Factory does not allow specification of file header line, only a boolean option to determine if it is in the first row. These scripts process formatted excel files so they can be read into ADF with first row as header
